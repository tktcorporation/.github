{
  "name": "tktcorporation-github",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "version": "changeset version",
    "release": "cd packages/create-devenv && npm run build && npm publish --provenance --access public"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8"
  }
}
